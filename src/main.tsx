import React from 'react'
import ReactDOM from 'react-dom/client'
import { SalesStudio } from '../real-estate-sales-ai/client/index.ts'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><SalesStudio /></React.StrictMode>)
